pub mod attractions;
// Test Options
// Show options for the cargo test command: `cargo test --help`
// Only run unit tests: `cargo test --lib`
// Only run integration tests: `cargo test --test '*'`
// Only run documentation tests: `cargo test --doc`

// Show options for the test binary itself: `cargo test -- --help`
// Reduce visual clutter of test output: `cargo test -- --format terse`
